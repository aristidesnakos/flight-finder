import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#40e0d0',  // turquoise
    },
    secondary: {
      main: '#f44336', // red, you can change this if you want
    },
  },
});

export default theme;
